#include <stdio.h>

int main()
{
    int n,i,a=0,b=1,out;
    printf("Enter n:");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        out=a+b;
        a=b;
        b=out;
        printf("%d\t",out);
    }
    return 0;
}