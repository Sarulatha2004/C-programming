#include <stdio.h>

int main()
{
    int n,i,sum;
    printf("Enter n:");
    scanf("%d",&n);
    sum=0;
    for(i=1;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("The sum of natural numbers upto %d is %d:",n,sum);
    return 0;
}