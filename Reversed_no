#include <stdio.h>

int main()
{
   int rev,rem,n;
   printf("Enter n:");
   scanf("%d",&n);
   rev=0;
   while(n!=0)
   {
       rem=n%10;
       rev=rev*10+rem;
       n=n/10;
   }
   printf("The reversed no is %d",rev);
   return 0;
}